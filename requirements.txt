# Production dependencies
mcp>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.24.0

# Development dependencies
# To install: pip install -r requirements.txt -e .[dev]
# See pyproject.toml for full dependency specs

